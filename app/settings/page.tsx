'use client';
import {useState} from 'react';
import {ArrowLeft, Check, Shield, UserRound} from 'lucide-react';
export default function Settings(){const [name,setName]=useState('');const [saved,setSaved]=useState(false);return <main className="min-h-screen px-6 py-8"><div className="mx-auto max-w-3xl"><a href="/dashboard" className="inline-flex items-center gap-2 text-sm text-white/40"><ArrowLeft size={15}/>Dashboard</a><h1 className="mt-8 text-3xl font-semibold">Settings</h1><p className="mt-2 text-sm text-white/40">Manage your profile and workspace preferences.</p><section className="mt-8 rounded-3xl border border-white/10 bg-white/[.035] p-6"><div className="flex items-center gap-3"><UserRound className="text-violet-300"/><div><h2 className="font-semibold">Profile</h2><p className="text-xs text-white/35">This is the name shown in your workspace.</p></div></div><label className="mt-6 block text-xs text-white/40">Display name</label><input value={name} onChange={e=>setName(e.target.value)} placeholder="Your name" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 p-3 outline-none focus:border-violet-400/50"/><button onClick={()=>setSaved(true)} className="mt-4 rounded-xl bg-white px-4 py-2 text-sm font-semibold text-black">Save changes</button>{saved&&<span className="ml-3 text-xs text-emerald-300"><Check size={14} className="mr-1 inline"/>Saved locally</span>}</section><section className="mt-5 rounded-3xl border border-white/10 bg-white/[.035] p-6"><div className="flex items-center gap-3"><Shield className="text-violet-300"/><div><h2 className="font-semibold">Security</h2><p className="text-xs text-white/35">API provider secrets stay server-side and are never shown here.</p></div></div><div className="mt-5 rounded-xl bg-black/20 p-4 text-sm text-white/55">For production, configure Supabase Auth, your server-side provider keys, Stripe secrets and webhook signing secret in your hosting environment.</div></section></div></main>}
